request({
    api: 'A0'
}, (result)=> {
    console.log(result);
})

this.request({
    api: 'A0',
    params: params
}, function (res) {
    calcFee(res.data.count);
})

_this.request({
    api: 'A0',
    params: params
}, function (res) {
    calcFee(res.data.count);
})

that.request({
    api: 'A0',
    params: params
}, function (res) {
    calcFee(res.data.count);
})